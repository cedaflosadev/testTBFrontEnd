import renderer from 'react-test-renderer';

import MainTable from '../components/main/MainTable';

it('changes the class when hovered', () => {
    const component =
        <MainTable ></MainTable>
    const result = "3"


    expect("3").toBe(result);

});

it('changes windows size', () => {
    const component =
        <MainTable ></MainTable>
    const result = 5 + 1

    expect(6).toBe(result);

});

it('changes in module', () => {
    const component =
        <MainTable ></MainTable>

    const result = "3"
    expect("3").toBe(result);

});

it('changes the class at the class', () => {
    const component =
        <MainTable ></MainTable>

    const result = "5"
    expect("5").toBe(result);

});

it('changes the class ', () => {
    const component =
        <MainTable ></MainTable>
    const result = "3"

    expect("3").toBe(result);

});

